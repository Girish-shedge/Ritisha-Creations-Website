# Copy to .env.local for local builds. Set the same names on Vercel.

GOOGLE_API_KEY=
GOOGLE_DRIVE_FOLDER_ID=1blEF1JY8k4fGg66R_O1ZcSyN6wFHz9W8

# Required if your API key uses HTTP referrer restrictions (Node has no browser referer).
# Use one of the URLs allowed on the key, e.g. http://localhost:5173/
GOOGLE_API_REFERER=http://localhost:5173/
